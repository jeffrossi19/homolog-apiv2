# Homolog-API
📄 **Desenvolvido para tornar a homologação de solicitações AnyMarket mais rápida e eficiente!**

## Descrição
-O **Homolog-API** é uma aplicação web desenvolvida para simplificar e acelerar o processo de homologação de solicitações para a API AnyMarket.
-Ele permite que os Analista/Homologador validem rapidamente a estrutura dos dados JSON enviados para diferentes endpoints, garantindo a conformidade com as especificações da API.
-Com esta ferramenta, o tempo de validação da homologação foi reduzido em até **90%**.

## Funcionalidades
### ✅ Validação de Estrutura JSON
- Permite colar solicitações JSON diretamente em uma área de texto.
- Valida se a estrutura do JSON corresponde aos campos esperados para o endpoint selecionado.

### 🔄 Seleção de Método e Endpoint
- Suporta métodos HTTP **POST** e **PUT**.
- Fornece uma lista suspensa para selecionar o endpoint da API AnyMarket a ser validado, com diferentes opções para cada método.

### ⚡ Feedback Instantâneo
- Exibe mensagens de erro claras se campos obrigatórios estiverem ausentes.
- Indica sucesso quando todos os campos esperados estão presentes.

### 🎨 Interface Intuitiva
- Interface de usuário simples e fácil de usar, baseada em **HTML** e **JavaScript**.
- Permite que os desenvolvedores validem as solicitações de forma rápida e eficiente.

### ⏳ Redução de Tempo
- Reduziu em até **90%** o tempo necessário para a validação, otimizando o processo de homologação.

## 🔧 Como Usar
1. **Acesso:** Abra o arquivo HTML em seu navegador da web.
2. **Cole o JSON:** Cole a solicitação JSON que deseja validar na área de texto fornecida.
3. **Selecione o Método:** Escolha o método HTTP (**POST** ou **PUT**) na lista suspensa.
4. **Selecione o Endpoint:** Escolha o endpoint da API AnyMarket correspondente à sua solicitação.
5. **Validar:** Clique no botão **"Validar"**.
6. **Analise os Resultados:** Verifique a mensagem de erro ou sucesso exibida abaixo da área de texto.

## 📚 Tecnologias Utilizadas
- **HTML**
- **CSS**
- **JavaScript**

## 🔬 Estrutura do Código
- O código **HTML** fornece a interface do usuário.
- O **JavaScript** realiza a validação do JSON e a lógica de seleção de endpoints.
- A função **validateRequest()** analisa o JSON inserido, verifica os campos esperados com base no método e endpoint selecionados e exibe os resultados na tela.
- A função **updateEndpoints()** preenche as opções do endpoint com base no método selecionado.

---


